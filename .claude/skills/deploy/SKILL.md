---
name: deploy
description: 만든 앱을 사내 Coolify에 배포할 때 사용. 환경변수(.env)를 Coolify에 옮겼는지 체크리스트로 확인하고, 데이터베이스 연결과 배포 순서를 안내한다. "배포", "올려서 공개", "Coolify", "/deploy" 라고 할 때 사용.
---

# Coolify 배포 도우미 (deploy)

내 노트북에서만 돌던 앱을 **회사 누구나 접속하는 주소**로 올립니다.
가장 흔한 배포 실패는 **"비밀 값(.env)을 Coolify에 안 넣어서"** 생깁니다. 그걸 체크리스트로 먼저 잡아줍니다.

## 기본 정보 (사내 Coolify)

- 관리 콘솔: **https://apps.gripcorp.co**
- **계정이 없으면 직접 가입하지 말고, 크루 리더에게 초대를 요청**하도록 안내합니다. (직접 가입하면 "OO's Team"이 따로 생겨 중앙 관리가 깨짐 — 모든 작업은 **`Grip` 팀** 안에서 합니다)
- 앱 주소(Domains) 규칙: **`<이름>.apps.gripcorp.co`** (예: `starter.apps.gripcorp.co`)

## 순서

1. **올리기 전에 안전하게 저장**
   - 먼저 `/safe-push`로 코드를 깃에 안전하게 올렸는지 확인합니다. (Coolify는 깃 저장소를 가져와 배포합니다)

2. **환경변수 체크리스트 만들기 (가장 중요)**
   - `.env` 파일에서 **키 이름만** 읽어옵니다. (예: `DATABASE_URL`, `METABASE_URL`, `METABASE_API_KEY`)
   - **값은 절대 화면에 출력하지 않습니다.** 이름 목록만 보여줍니다.
   - 사용자에게 안내: "Coolify → 내 앱 → **Environment Variables** 에 아래 이름들을 **같은 이름으로** 넣으세요."
     - [ ] `DATABASE_URL` — **데이터 저장 기능을 쓸 때만.** 안 쓰면 비워도 되고, 그러면 화면만 배포됩니다.
     - [ ] `METABASE_URL`, `METABASE_API_KEY` — 회사 데이터를 쓸 때만 (리더가 발급)
   - "값은 어디서 와요?"라고 물으면: DB 주소는 Coolify Postgres 생성 시 제공, Metabase 값은 리더가 발급.

3. **데이터베이스 (선택 — 데이터를 저장할 때만)**
   - 데이터 저장이 필요 없으면 이 단계는 건너뜁니다. `DATABASE_URL` 없이도 앱은 정상 배포됩니다.
   - 저장이 필요하면: Coolify에서 **PostgreSQL을 하나 추가**(원클릭)하고 연결 주소를 `DATABASE_URL`에 넣습니다.
   - 그러면 배포될 때마다 데이터 구조가 **자동으로 DB에 반영(`prisma db push`)**됩니다(Dockerfile). 따로 실행할 필요 없습니다. ("마이그레이션"을 따로 돌릴 필요 없음)

4. **배포 방식·주소 확인**
   - Coolify에서 이 앱은 **Dockerfile** 로 빌드됩니다(저장소에 포함됨). 빌드 방식이 Dockerfile로 잡혀 있는지 확인하도록 안내합니다.
   - 포트는 `3000` 입니다.
   - Domains는 **`<이름>.apps.gripcorp.co`** 형태로 설정하도록 안내합니다 (예: `starter.apps.gripcorp.co`).
   - 프로젝트가 **`Grip` 팀** 안에 만들어졌는지 확인합니다.

5. **배포 & 확인**
   - 배포를 트리거하고, 빌드 로그에서 에러가 없는지 봅니다. 끝나면 접속 주소를 열어 화면이 뜨는지 확인합니다.
   - 데이터 저장 기능을 켰는데 안 되면 → `DATABASE_URL`이 비었거나 틀린 경우. 2~3단계를 다시 확인합니다. (저장을 안 쓸 거면 화면만 떠도 정상)

## 규칙

- `.env`의 **실제 값을 채팅/로그에 출력하지 않습니다.** 이름만 다룹니다.
- 막히면 [Coolify 가이드](https://coolify.io/docs) 또는 크루 리더에게 문의하도록 안내합니다.
