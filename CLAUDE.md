# 이 프로젝트에서 Claude가 지켜야 할 규칙

이 프로젝트는 **비전공자(개발 경험 없는 사람)** 가 Claude Code와 함께 만드는 서비스입니다.
아래 규칙을 **항상, 예외 없이** 지키세요. 사용자가 명시적으로 다른 지시를 하면 그 지시가 우선합니다.

## 0. 말투 · 설명

- **항상 한국어**로, 비전공자가 알아듣게 쉽게 설명합니다. 전문용어를 쓰면 한 줄로 풀어줍니다.
- 무엇을, 왜 하는지 짧게 말하고 진행합니다. 사용자가 직접 해야 할 일이 있으면 멈추고 안내합니다.

## 1. 보안 — 가장 중요 (어기면 안 됨)

- **비밀 값(API 키·비밀번호·토큰)을 코드에 직접 쓰지 않습니다.** 반드시 `.env` 파일에 넣고 `process.env`로 불러옵니다.
- `.env`, `.env.local` 같은 비밀 파일을 **절대 깃에 커밋하지 않습니다.** (`.env.example`만 올라갑니다)
- 채팅창에 사용자가 키를 붙여넣으면, 코드/로그에 남기지 말고 `.env`에 넣도록 안내합니다.
- **커밋·푸시 전에는 항상 `/safe-push`** 를 먼저 돌립니다. 직접 `git push` 하기 전에 안내하세요.
- 보안 검사(pre-commit hook)를 `--no-verify` 등으로 **우회하지 않습니다.**

## 2. 작업 시작 시 자동 점검 (안전장치 자가 복구)

이 저장소에는 비밀 값 유출을 막는 커밋 검사기가 있습니다. **첫 작업을 시작할 때 한 번 확인**하세요:

- `git config core.hooksPath` 값이 `.githooks` 인지 확인합니다.
- 아니라면 `git config core.hooksPath .githooks` 를 실행하고, `.githooks/pre-commit` 에 실행 권한(`chmod +x`)을 줍니다.
- 사용자가 초기 설정 프롬프트를 깜빡했어도 이 점검으로 안전장치가 켜집니다.

## 3. 데이터 — 회사 데이터 vs 내 앱 데이터 (헷갈리지 말 것)

데이터는 **두 종류**이고 다루는 길이 다릅니다.

| 종류 | 예 | 다루는 법 |
|---|---|---|
| **내 앱 데이터** | 방명록, 사용자가 만든 글, 앱 설정 등 | 내 Postgres + **Prisma** (`lib/prisma.ts`) 로 자유롭게 저장/조회 |
| **회사 데이터** | 매출·셀러·방송·고객 등 사내 데이터 | 오직 **Metabase API** (`lib/metabase.ts`) 로 **읽기 전용** 조회 |

- **회사 데이터베이스에 직접 접속하는 코드는 절대 작성하지 않습니다.** (회사 DB 커넥션 스트링, 회사 DB로의 `psql` 등 금지)
- 회사 데이터가 필요하면 `lib/metabase.ts`를 쓰고, 가장 안전한 방법은 Metabase에 저장된 "질문(카드)"의 결과를 가져오는 것입니다.
- 자세한 규칙은 `.claude/skills/metabase-data` 스킬을 따릅니다.

> 헷갈릴 때 기준: "이 데이터를 **내가 새로 만들어 저장**하나?" → 내 앱 DB(Prisma). "**회사가 이미 가진 데이터를 읽나?**" → Metabase API.

## 4. 데이터베이스 작업 (Prisma) — 쉽게, 자동으로

비전공자에게 **"마이그레이션"이라는 개념을 설명하지 않습니다.** 사용자는 "~를 저장되게 해줘"라고만 말하고, 나머지는 Claude가 처리합니다.

- 데이터 저장이 필요하면: `prisma/schema.prisma`에 모델을 추가/수정한 뒤 **`npx prisma db push`** 를 실행해 DB에 반영합니다. (`migrate`가 아니라 `db push` — 명령 하나로 끝, 마이그레이션 파일·이름 불필요)
- 사용자에게는 "**데이터 보관함 모양을 코드에 맞춰 반영했어요**"처럼 쉽게 설명합니다. "migration", "마이그레이션" 같은 단어는 쓰지 않습니다.
- `db push`가 데이터 손실 가능성을 경고하면(컬럼 삭제·타입 변경 등), **그대로 진행하지 말고 사용자에게 무엇이 지워질 수 있는지 먼저 알리고 확인**받습니다.
- 첫 앱은 DB 없이도 화면이 떠야 합니다. `DATABASE_URL`이 없으면 저장 기능만 꺼지고 화면은 정상 동작하도록 유지합니다.

## 5. 이 Next.js는 최신 버전입니다 (중요)

이 프로젝트의 Next.js는 **최신 버전**이라, 예전 방식과 다를 수 있습니다.
코드를 쓰기 전에 필요하면 `node_modules/next/dist/docs/` 의 관련 문서를 확인하고, deprecation 경고를 존중하세요.
React/Next 모범사례는 `.claude/skills/react-best-practices`, 컴포넌트 구성은 `.claude/skills/composition-patterns` 스킬을 참고합니다.

## 6. 배포 (Coolify)

- 배포는 **`/deploy`** 스킬을 따릅니다. 가장 흔한 실패는 `.env`의 값을 **Coolify Environment Variables에 안 넣어서** 생깁니다 — 항상 체크리스트로 확인시킵니다.
- 배포 시 데이터베이스 마이그레이션은 컨테이너 시작 때 자동 적용됩니다(Dockerfile).

## 7. 안전한 작업 습관

- 파일을 **지우거나 덮어쓰기 전에** 사용자에게 확인합니다.
- 한 덩어리 작업이 끝나면 `/safe-push`로 자주 저장(백업)하도록 권합니다.
